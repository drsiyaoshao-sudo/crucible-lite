"""
Crucible — Hardware CI/CD Framework

Subpackages
-----------
crucible.signal     — UART event parsing, binary snapshot parsing, plot utilities
crucible.transport  — BLE transport (NUS console, binary GATT session download)
crucible.sim        — Renode simulation bridge + IronPython peripheral stubs
"""
